#include <iostream>
#include <unordered_set>
using namespace std;

bool badValue(int x) {
    return x % 2 == 1;
}

int main(int argc, char* argv[]) {
    unordered_set<int> c;
    for (unordered_set<int>::iterator i = c.begin(); i != )
    return 0;
}